import { Routes } from '@angular/router';
import { ElixirsComponent } from './component/elixirs/elixirs.component';

export const routes: Routes = [
    { path: '', component: ElixirsComponent }, // Default route
];